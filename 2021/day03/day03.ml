(*
list of bin numbers to decode

generate gamma rate and epsilon rate

to check:
  - power consumption = gamma_rate * epsilon_rate

gamma rate = max 0's or 1's per cols -> bin num -> dec
epsilon rate = inverse

*)