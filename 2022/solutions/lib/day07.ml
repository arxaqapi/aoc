let filename = "data/07_input"


(*
sum of dir where size is bigger than => 10_000   
count nested files also

$ ls => list of files:
  328909 dwvphbm = size name
  dir akjeliub = type name
$ cd .. => go back
$ cd azyge => cd into dir
*)

